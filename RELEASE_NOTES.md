# 公文排版助手 V1.3

发布日期：2026-05-12

本版本重点完成了 V1.3 界面的放大重制、外接显示器/DPI 兼容修复，以及 Windows 便携版自动编译发布流程。下载 release 中的 Windows portable 压缩包后，解压并双击 `公文排版助手V1_3.exe` 即可运行，不需要在用户电脑上安装 Python 或项目依赖。

## 主要更新

- 界面版本升级为 V1.3，窗口标题、应用版本和打包产物统一更新为 `公文排版助手 V1.3` / `公文排版助手V1_3`。
- 参考最初版界面比例重新放大主界面，整体窗口调整为更宽、更高的布局，左侧编辑区和右侧参数区都保留更舒展的阅读与操作空间。
- 字体、按钮、输入框、下拉框、页边距控件和各区域间距随界面整体放大，避免 V1.2.1 在较大字号下显得拥挤。
- 生成按钮颜色改为深灰绿色 `#5F7450`，与浅绿色参数卡片和灰绿色背景形成更稳定的视觉层次。
- 右侧参数区重新校准页面设置、段落设置、文档标题、一级标题、二级标题、正文和导出文档区域的对齐关系。
- 修复“正文”标题在部分显示模式下位置漂移的问题，改为在卡片内部用独立控件定位，避免跨显示器/DPI 缩放时跑到界面上方。
- 修复底部联系方式在外接显示器“复制”和“仅第二屏幕”模式下跑到中间的问题，改为底部布局右侧锚定。
- 加入 Windows Per-Monitor DPI Awareness，提升笔记本外接显示器、不同缩放比例和投影模式下的界面稳定性。

## 参数与排版默认值

- 默认字体调整为更适合公文排版的组合：文档标题、二级标题、正文默认为仿宋，一级标题默认为黑体。
- 默认字号随界面调整为更大的显示规格，减少控件文字被裁切的概率。
- 段落设置中的固定行距单位显示为“磅”，与中文公文排版习惯保持一致。
- 保留导入文档后自动清除原格式、仅保留文字和段落结构的处理方式。
- 保留文档标题、一级标题、二级标题、正文的自动识别和导出 `.docx` 功能。

## 构建与发布

- 新增/完善 GitHub Actions Windows 工作流：推送 `python` 分支时自动编译 Windows 便携版并上传 artifact。
- 推送 `v*` 标签时，工作流会自动编译 exe、打包 `公文排版助手V1_3-windows-portable.zip`，并创建 GitHub Release。
- Release 附件内包含完整运行目录，用户解压后可直接双击 exe 使用，无需安装 Python、PySide6、python-docx 或 PyInstaller。

## 已验证

- 本地通过 `python -m compileall app` 语法检查。
- 本地通过 PyInstaller 成功生成 Windows 便携版。
- 已用截图验证默认宽度和模拟宽屏下，底部联系方式均保持右侧对齐。
