# 公文排版助手 V1.3.1

发布日期：2026-05-26

这是一个面向正式公文输出质量的版本。V1.3.1 在 V1.3 的界面基础上，补齐了重点字段强调、正文两端对齐、标题后空行、中英文标点自动适配和字体随包交付能力，目标是让普通用户解压后即可得到更稳定、更接近实际公文要求的 `.docx` 输出。

## 下载与运行

请下载本 Release 附件中的 Windows 便携版压缩包：

```text
公文排版助手V1_3_1-windows-portable.zip
```

使用方式：

1. 解压压缩包。
2. 进入 `公文排版助手V1_3_1` 文件夹。
3. 双击 `公文排版助手V1_3_1.exe`。

普通用户不需要安装 Python、PySide6、python-docx、PyInstaller 或其他开发依赖。

## 相比 V1.3 的主要变化

### 1. 新增重点字段功能

主界面新增“重点字段”按钮，用户可以单独设置需要强调的字段。

默认重点字段包括：

- 分析认为
- 一是
- 二是
- 三是
- 四是
- 五是

重点字段支持：

- 新增字段。
- 删除字段。
- 保存用户设置，下次打开软件继续保留。
- 单独设置字体。
- 单独设置字号。
- 勾选或取消加粗。

导出 `.docx` 时，软件会自动匹配正文中的重点字段，并只对匹配到的文字应用专用格式。

默认重点字段格式为：

```text
仿宋_GB2312，小二，加粗
```

### 2. 输出文档正文改为两端对齐

正文段落导出时使用两端对齐，更符合正式材料的阅读和排版习惯。

一级标题、二级标题和文档标题仍按各自规则处理，不受正文对齐方式影响。

### 3. 标题后自动空一行

导出的 `.docx` 中，文档标题后会自动插入一个空白段落。

这个空白段落具有以下特点：

- 没有任何文字。
- 使用正文段落格式。
- 与正文保持相同固定行距。
- 不会写入空格或占位字符。

### 4. 中英文标点自动适配

导出前会自动根据语境调整标点：

- 中文语境使用中文标点。
- 英文语境使用英文标点。
- 中文句子中夹带英文产品名时，句末标点仍按中文语境处理。

例如，中文语境中的：

```text
最后无缝过渡到产品名称 “Codepilot".
```

导出时会整理为：

```text
最后无缝过渡到产品名称 “Codepilot”。
```

数字序号和小数点也做了保护，避免被误转为中文句号。

### 5. 默认字体更新为公文常用组合

V1.3.1 默认排版参数调整为：

| 类型 | 字体 | 字号 |
| --- | --- | --- |
| 文档标题 | 方正小标宋简体 | 二号 |
| 一级标题 | 黑体 | 小二 |
| 二级标题 | 楷体_GB2312 | 小二 |
| 正文 | 仿宋_GB2312 | 小二 |
| 重点字段 | 仿宋_GB2312 | 小二，加粗 |

### 6. 软件随包内置常用字体

为减少用户电脑缺少字体导致的输出偏差，本版本随软件打包以下字体：

- 方正小标宋简体
- 黑体
- 楷体_GB2312
- 仿宋_GB2312

软件启动时会自动注册这些字体。界面下拉框中直接显示真实字体名，不再额外显示“软件自带”等提示文字，保持界面简洁。

字体管理规则：

- “加载本地字体”面板不会显示软件自带字体，避免重复加载。
- “删除字体”面板可以隐藏这些字体在下拉菜单中的选项。
- 删除只是隐藏软件选项，不会删除软件包或系统中的字体文件。
- Windows 字体注册使用临时副本，避免锁住打包输出目录里的字体文件。

### 7. 弹窗视觉统一

字体加载、字体删除和重点字段弹窗的列表区域统一为：

- 浅色背景。
- 浅绿边框。
- 深色文字。
- 选中和悬停状态与主界面风格一致。

重点字段弹窗保留了与主界面一致的按钮、输入框、下拉框和加粗勾选样式。

### 8. 构建和发布流程更新

本版本继续使用 GitHub Actions 自动编译 Windows 便携版。

构建输出目录统一为：

```text
dist/公文排版助手V1_3_1
```

发布时会压缩完整便携目录，而不是只发布单个 exe，确保 `_internal` 依赖、图标资源、下拉箭头和自带字体都包含在 Release 附件中。

## 已验证内容

本地发布前已完成以下检查：

- Python 源码语法检查通过。
- `git diff --check` 通过，仅存在 Windows 换行转换提示。
- 默认字体下拉框显示为纯字体名。
- 重点字段弹窗默认字体为 `仿宋_GB2312`。
- “加载本地字体”面板不会出现软件自带字体。
- “删除字体”面板可显示并隐藏软件自带字体选项。
- 导出 `.docx` 写入真实字体名，不写入界面辅助说明。
- 正文段落为两端对齐。
- 标题后空白段落为空文本，并使用正文格式。
- 中文语境中的英文产品名句尾使用中文句号。
- 最终打包目录内包含四个自带字体文件。

GitHub Actions 编译完成后，本 Release 附件即为工作流生成的正式便携包。

## 升级说明

旧版本不会被删除，历史 Release 和历史标签继续保留。

如果用户此前保存过旧默认参数，新版启动时可能继续读取旧配置。需要恢复 V1.3.1 内置默认字体组合时，请点击主界面的“恢复默认参数”。

## 文件名

- 版本标签：`v1.3.1`
- 便携目录：`公文排版助手V1_3_1`
- 主程序：`公文排版助手V1_3_1.exe`
- Release 附件：`公文排版助手V1_3_1-windows-portable.zip`
