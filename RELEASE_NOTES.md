# 公文排版助手 V1.3.1

发布日期：2026-05-26

覆盖更新时间：2026-05-27

这是一个面向正式公文输出质量的版本。V1.3.1 在 V1.3 的界面基础上，补齐了重点字段强调、正文两端对齐、标题后空行和中英文标点自动适配，并改为使用用户电脑本地字体输出 `.docx`。本版本同时统一默认字体组合、优化本地字体管理、优化重点字段弹窗视觉，并将配套 skill 拆分到独立仓库维护。

2026-05-27 覆盖修订取消了历史字体别名替换规则。软件现在会保留 `方正小标宋简体`、`方正小标宋_GBK`、`楷体_GB2312`、`仿宋_GB2312` 等本机字体的真实名称，不再把它们自动替换为 `黑体` 或 `仿宋`。

## 下载与运行

请下载本 Release 附件中的 Windows 便携版压缩包：

```text
公文排版助手V1_3_1-windows-portable.zip
```

使用方式：

1. 解压压缩包。
2. 进入 `公文排版助手V1_3_1` 文件夹。
3. 双击 `公文排版助手V1_3_1.exe`。

普通用户不需要安装 Python、PySide6、python-docx、PyInstaller 或其他开发依赖。

## 相比 V1.3 的主要变化

### 1. 新增重点字段功能

主界面新增“重点字段”按钮，用户可以单独设置需要强调的字段。

默认重点字段包括：

- 分析认为
- 一是
- 二是
- 三是
- 四是
- 五是

重点字段支持：

- 新增字段。
- 删除字段。
- 保存用户设置，下次打开软件继续保留。
- 单独设置字体。
- 单独设置字号。
- 勾选或取消加粗。

导出 `.docx` 时，软件会自动匹配正文中的重点字段，并只对匹配到的文字应用专用格式。

默认重点字段格式为：

```text
仿宋，小二，加粗
```

### 2. 输出文档正文改为两端对齐

正文段落导出时使用两端对齐，更符合正式材料的阅读和排版习惯。

一级标题、二级标题和文档标题仍按各自规则处理，不受正文对齐方式影响。

### 3. 标题后自动空一行

导出的 `.docx` 中，文档标题后会自动插入一个空白段落。

这个空白段落具有以下特点：

- 没有任何文字。
- 使用正文段落格式。
- 与正文保持相同固定行距。
- 不会写入空格或占位字符。

### 4. 中英文标点自动适配

导出前会自动根据语境调整标点：

- 中文语境使用中文标点。
- 英文语境使用英文标点。
- 中文句子中夹带英文产品名时，句末标点仍按中文语境处理。

例如，中文语境中的：

```text
最后无缝过渡到产品名称 “Codepilot".
```

导出时会整理为：

```text
最后无缝过渡到产品名称 “Codepilot”。
```

数字序号和小数点也做了保护，避免被误转为中文句号。

### 5. 默认字体更新为公文常用组合

V1.3.1 默认排版参数调整为：

| 类型 | 字体 | 字号 |
| --- | --- | --- |
| 文档标题 | 黑体 | 二号 |
| 一级标题 | 黑体 | 小二 |
| 二级标题 | 黑体 | 小二 |
| 正文 | 仿宋 | 小二 |
| 重点字段 | 仿宋 | 小二，加粗 |

字体下拉菜单的默认候选也同步精简为：

- 黑体
- 仿宋

新版会自动保持当前默认字体组合，避免下拉菜单继续出现非当前默认字体项。

### 6. 本地字体管理

本版本使用用户电脑本地字体输出 `.docx`，字体下拉菜单默认保留当前版本推荐字体组合。

字体名称直接显示真实字体名，保持界面简洁。

字体管理规则：

- “加载本地字体”面板直接读取 Windows 本地字体列表。
- “删除字体”面板可以隐藏下拉菜单中的字体选项。
- 删除只是隐藏软件选项，不会删除系统字体文件。
- 加载本地字体和导出 `.docx` 时保留真实字体名。
- 已取消历史兼容别名：`方正小标宋简体`、`方正小标宋_GBK`、`楷体_GB2312`、`仿宋_GB2312` 不再被改写为其他字体。

### 7. 弹窗视觉统一和加粗选项优化

字体加载、字体删除和重点字段弹窗的列表区域统一为：

- 浅色背景。
- 浅绿边框。
- 深色文字。
- 选中和悬停状态与主界面风格一致。

重点字段弹窗保留了与主界面一致的按钮、输入框、下拉框和加粗勾选样式。

重点字段弹窗中的“加粗”选项改为带边框的选择框样式，边框颜色、圆角、背景和悬停效果与下拉框保持一致；字体、字号、加粗选项的文字高度和控件上下边缘已对齐。

### 8. Skill 独立仓库

配套 skill 不再随 EXE 应用仓库一起提交，已拆分到独立仓库维护：

```text
https://github.com/SugarWanders/gongwen_paiban_skill
```

skill 名称为 `gongwen_format_V1_3_1`，与本版本 EXE 文件名保持一致，方便识别同一批次发布内容。skill 运行时仅依赖用户本地电脑中已经安装的字体，并在生成 `.docx` 时写入对应字体名称。

### 9. 构建和发布流程更新

本版本继续使用 GitHub Actions 自动编译 Windows 便携版。

构建输出目录统一为：

```text
dist/公文排版助手V1_3_1
```

发布时会压缩完整便携目录，而不是只发布单个 exe，确保 `_internal` 依赖、图标资源和下拉箭头都包含在 Release 附件中。

## 已验证内容

本地发布前已完成以下检查：

- Python 源码语法检查通过。
- `git diff --check` 通过，仅存在 Windows 换行转换提示。
- 默认字体下拉框只保留 `黑体` 和 `仿宋`。
- 重点字段弹窗默认字体为 `仿宋`。
- 重点字段弹窗“加粗”选项边框和下拉框高度对齐。
- 修改排版参数后会自动保存，下次启动时恢复为上次设置。
- “加载本地字体”面板读取 Windows 本地字体。
- “删除字体”面板可隐藏字体下拉菜单选项。
- 导出 `.docx` 写入真实字体名，不写入界面辅助说明。
- 已验证字体别名替换规则取消，`方正小标宋简体`、`楷体_GB2312`、`仿宋_GB2312` 会保持原名。
- 正文段落为两端对齐。
- 标题后空白段落为空文本，并使用正文格式。
- 中文语境中的英文产品名句尾使用中文句号。
- 最终输出目录使用本地字体管理方式。
- 配套 skill 已推送至独立仓库，并在说明中注明运行时依赖本地字体。

GitHub Actions 编译完成后，本 Release 附件即为工作流生成的正式便携包。

## 升级说明

旧版本不会被删除，历史 Release 和历史标签继续保留。

如果启动后仍沿用此前的个人排版参数，需要恢复 V1.3.1 默认字体组合时，请点击主界面的“恢复默认参数”。

## 文件名

- 版本标签：`v1.3.1`
- 便携目录：`公文排版助手V1_3_1`
- 主程序：`公文排版助手V1_3_1.exe`
- Release 附件：`公文排版助手V1_3_1-windows-portable.zip`
