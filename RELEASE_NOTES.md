# 公文排版助手 V1.3.2

发布日期：2026-07-30

V1.3.2 新增正文多余空格检查，导出公文时可以自动清理中文正文中的异常空格，同时保留英文、数字和单位所需间隔。下载 Windows 便携版压缩包后，解压并双击 `公文排版助手V1_3_2.exe` 即可运行，不需要安装 Python 或项目依赖。

## 主要更新

- “段落设置”新增“检查正文空格”选项，默认开启并记住用户选择。
- 清理仅作用于自动识别为正文的段落，不修改编辑区、文档标题、一级标题和二级标题。
- 中文字符之间及中文标点周围的多余空格会被删除。
- 其他连续空格、Tab、全角空格和不间断空格会压缩为一个半角空格。
- 保留 `Microsoft Word`、`100 kg` 等英文、数字和单位所需的单个空格。
- 正文清理后继续匹配重点字段，保证被多余空格拆开的重点字段仍可应用专用格式。

## 参数与兼容

- “检查正文空格”默认开启，其勾选状态随默认模板自动保存。
- 旧版模板没有 `clean_body_spaces` 字段时自动按开启处理。
- 配置字段不是布尔值时安全回退为默认开启，不影响软件启动。
- V1.3.1 的字体名称、本地字体管理、重点字段、正文两端对齐、标题后空行和中英文标点适配规则继续保留。

## 下载与运行

构建附件名称：

```text
公文排版助手V1_3_2-windows-portable.zip
```

使用方式：

1. 下载并解压 Windows 便携版压缩包。
2. 进入 `公文排版助手V1_3_2` 文件夹。
3. 双击 `公文排版助手V1_3_2.exe`。

普通用户不需要安装 Python、PySide6、python-docx、PyInstaller 或其他开发依赖。

## 构建与发布

- 版本分支：`codex/v1.3.2`
- 推送 `python` 或 `codex/v*` 分支时，GitHub Actions 自动编译 Windows 便携版并上传 artifact。
- 推送 `v*` 标签时，GitHub Actions 自动创建 GitHub Release 并附加完整便携版压缩包。
- 本地构建目录：`dist/公文排版助手V1_3_2`
- 主程序：`公文排版助手V1_3_2.exe`

## 已验证

- 11 项 `unittest` 自动化测试通过。
- 正文空格清理、开关关闭、旧模板兼容和重点字段匹配验证通过。
- 实际 `.docx` 生成和段落内容回读验证通过。
- Python 源码语法检查和 `git diff --check` 通过。
- 100% / 125% / 150% DPI 下复选框无文字裁切和控件重叠。
- 使用 Python 3.14.3 / PyInstaller 6.19.0 完成本地 Windows 便携版构建，EXE 隐藏启动冒烟测试通过。
