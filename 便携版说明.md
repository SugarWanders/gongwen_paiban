# 公文排版助手便携版说明

## 1. 交付方式

软件采用 Windows 绿色版交付，不提供安装程序。

最终交付物为一个可直接打开的文件夹，例如：

```text
dist/
  公文排版助手/
    公文排版助手.exe
    ...
```

用户只需要：

1. 打开 `公文排版助手` 文件夹
2. 双击 `公文排版助手.exe`
3. 直接开始使用

## 2. 便携版特点

- 无需安装
- 无需管理员权限
- 可直接复制到桌面、U 盘或任意目录运行
- 删除文件夹即可完成卸载

## 3. 构建方式

在项目根目录执行：

```powershell
python -m pip install -r requirements.txt
./build_portable.ps1
```

构建完成后，绿色版输出目录为：

```text
dist/公文排版助手/
```

主程序为：

```text
dist/公文排版助手/公文排版助手.exe
```

## 4. 说明

- 当前方案使用 `PyInstaller` 打包为 `one-folder` 绿色版，而不是安装包
- 选择文件夹版而不是单文件版，是为了减少启动解压等待时间，并降低被安全软件误报的概率
