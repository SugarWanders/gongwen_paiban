# 开发文档

| 项目 | 内容 |
| --- | --- |
| 文档编号 | GWP-DEV-001 |
| 文档版本 | V1.3.1 |
| 更新日期 | 2026-05-26 |
| 适用范围 | 公文排版助手功能、技术结构、构建和维护说明 |

## 项目目标

公文排版助手用于将用户导入或粘贴的文字内容重新排版，并导出为符合指定参数的 `.docx` 文档。项目重点是提供一个免安装、可双击运行的 Windows 桌面工具。

## 技术栈

- Python
- PySide6：桌面界面
- python-docx：读取和生成 `.docx`
- PyInstaller：生成 Windows 便携版
- GitHub Actions：自动编译和发布 Release

## 目录结构

```text
app/
  main.py                         应用入口、DPI 设置、图标、版本和字体注册
  models/settings.py              默认模板、字号映射、重点字段配置
  services/bundled_fonts.py        软件自带字体注册和显示名处理
  services/document_importer.py    文档导入
  services/export_service.py       docx 导出、标点归一、重点字段格式应用
  services/focus_field_store.py    重点字段配置保存
  services/font_service.py         本地字体读取
  services/font_store.py           字体偏好保存
  services/save_path_store.py      默认保存路径保存
  services/template_store.py       默认模板保存
  services/title_classifier.py     段落标题识别
  ui/main_window.py                主窗口、弹窗和交互逻辑
  ui/styles.py                     QSS 样式
  ui/assets/                       下拉和数字选择箭头资源
  fonts/                           随软件打包的字体文件
build_portable.ps1                 本地便携版构建脚本
portable_build.spec                PyInstaller 配置
requirements.txt                   Python 依赖
RELEASE_NOTES.md                   GitHub Release 说明
```

## 核心流程

1. 用户导入 `.docx` / `.txt`，或直接在编辑区粘贴文字。
2. 软件清除原文档格式，仅保留文字和段落结构。
3. 软件按规则识别段落类型。
4. 用户设置页边距、固定行距、字体、字号和重点字段。
5. 用户选择或确认导出文件夹。
6. 软件生成排版后的 `.docx`。
7. 导出成功后，用户可打开文档或在文件夹中显示文档。

## 自动识别规则

- 第一段识别为文档标题。
- 以“一、二、三”等开头的段落识别为一级标题。
- 以“（一）（二）（三）”等开头的段落识别为二级标题。
- 其他段落识别为正文。

## 默认排版参数

- 页边距：上 36 毫米，下 36 毫米，左 27 毫米，右 27 毫米。
- 固定行距：30 磅。
- 文档标题：方正小标宋简体，二号。
- 一级标题：黑体，小二。
- 二级标题：楷体_GB2312，小二。
- 正文：仿宋_GB2312，小二。
- 重点字段：仿宋_GB2312，小二，加粗。

## 重点字段实现

重点字段配置位于 `FocusFieldConfig`：

- `fields`：字段列表。
- `style`：重点字段专用字体和字号。
- `bold`：是否加粗。

默认字段包括“分析认为”“一是”“二是”“三是”“四是”“五是”。

运行时配置保存在：

```text
config/focus_fields.json
```

导出时，`export_service.py` 会按字段长度倒序匹配正文中的重点字段，并将匹配文本拆成独立 run，以便只给匹配片段应用专用字体、字号和加粗。

## 标点处理

导出前会对文本执行标点归一：

- 中文语境使用中文标点。
- 英文语境使用英文标点。
- 中文句子中夹带英文产品名时，句末标点仍按中文语境处理。
- 数字序号和小数点不会被误转为中文句号。

该逻辑在 `export_service.py` 中实现，核心函数为 `normalize_punctuation_for_output`。

## docx 导出规则

- 标题居中。
- 标题后若存在后续段落，自动增加一个空白段落。
- 空白段落无文字，段落格式沿用正文。
- 正文段落使用两端对齐。
- 一级标题和二级标题使用对应标题格式。
- 重点字段匹配后应用专用格式。

## 字体管理

软件内置并打包以下字体：

- 方正小标宋简体
- 黑体
- 楷体_GB2312
- 仿宋_GB2312

字体文件位于：

```text
app/fonts/
```

`bundled_fonts.py` 负责：

- 维护软件自带字体清单。
- 启动时向 Qt 注册字体。
- 在 Windows 会话中注册字体。
- 使用临时目录字体副本进行 Windows 注册，避免锁住 `dist` 中的字体文件。
- 规范化字体名，保证界面显示名和 `.docx` 写入字体名一致。

字体下拉框直接显示真实字体名，不显示额外标记。“加载本地字体”面板过滤自带字体，“删除字体”面板可隐藏自带字体选项。

## 保存路径和配置

- 默认模板：`config/default_template.json`
- 默认保存路径：`config/default_save_path.json`
- 字体偏好：`config/font_preferences.json`
- 重点字段：`config/focus_fields.json`

这些配置为运行时生成文件，不纳入仓库提交。

## 界面实现要点

主窗口由 `app/ui/main_window.py` 负责，样式由 `app/ui/styles.py` 负责。

V1.3.1 的关键界面参数：

- 默认窗口尺寸：`1030 x 613`
- 右侧参数面板宽度：`390px`
- 主卡片高度：`572px`
- 底部状态区高度：`31px`
- 主操作按钮颜色：`#5F7450`
- 字体下拉框宽度保持 V1.3 原始尺寸
- 弹窗列表使用浅色背景和浅绿边框

为适配外接显示器和不同缩放比例，应用在启动时启用 Windows DPI Awareness。

## 图标与打包

图标链路分为两层：

- 运行时窗口图标：`app/main.py` 中设置 `app.setWindowIcon(...)` 和 `window.setWindowIcon(...)`
- EXE 文件图标：`portable_build.spec` 中设置 `icon="icon.ico"`

PyInstaller 打包时携带：

- `icon.png`
- `icon.ico`
- `app/fonts`
- `app/ui/assets/down-arrow.svg`
- `app/ui/assets/up-arrow.svg`

## 本地运行

```powershell
python -m pip install -r requirements.txt
python app/main.py
```

## 本地构建

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build_portable.ps1
```

构建输出：

```text
dist/公文排版助手V1_3_1
```

构建脚本会清理 `dist` 下旧版本目录，并删除最终输出中的运行配置目录，避免旧配置覆盖新版默认值。

## 发布流程

GitHub Actions 工作流位于：

```text
.github/workflows/build-windows.yml
```

发布规则：

- 推送 `python` 分支时自动编译 Windows 便携版 artifact。
- 推送 `v*` 标签时自动编译、压缩并创建 GitHub Release。
- Release 正文使用 `RELEASE_NOTES.md`。

## 基础校验

提交前建议执行：

```powershell
python -m compileall app
git diff --check
```

涉及界面修改时，应生成真实界面截图，确认不存在文字裁切、控件重叠和 DPI 缩放问题。截图和测量报告属于生成物，提交前可清理。
