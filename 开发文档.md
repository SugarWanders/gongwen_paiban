# 文档重排版工具开发文档

## 1. 项目概述

开发一款运行在 Windows 上的桌面软件，用于将用户上传或粘贴的文本内容重新按指定规则排版，并导出为 `.docx` 文件。

软件支持两种输入方式：

- 上传文件：`.docx`、`.txt`
- 在可视化编辑窗口中直接粘贴或输入文字

系统在导入文件后会清除原有格式，仅保留文字内容和基础段落结构，并将内容展示在编辑窗口中供用户继续修改。用户随后设置页边距、行距、各级标题和正文字体字号等参数，生成新的排版文档。

软件交付方式采用 Windows 免安装绿色版，用户双击程序即可使用，不需要执行安装流程。

## 2. 产品目标

### 2.1 核心目标

- 支持 `.docx` 和 `.txt` 文件导入
- 清除原有样式并提取文本内容
- 提供基础可视化编辑能力
- 自动识别标题层级
- 支持自定义排版参数
- 导出规范化 `.docx` 文件

### 2.2 产品定位

本工具定位为“统一套版重排版工具”，而不是完整替代 Word 的通用文档编辑器。

首版重点解决：

- 将已有文字快速整理成规范格式
- 适配常见公文/汇报/通知类结构
- 降低人工逐段调格式的重复工作

## 3. 用户场景

### 3.1 典型场景

- 用户从 Word 中导入一篇内容杂乱的文档，希望快速统一为标准格式
- 用户从 TXT 文本导入内容后，按正式材料模板重新排版
- 用户先粘贴内容，再在软件中简单修改后直接生成 `.docx`

### 3.2 目标用户

- 行政办公人员
- 需要整理正式文稿的工作人员
- 频繁编制通知、报告、方案、总结的用户

## 4. V1 功能范围

### 4.1 支持的输入方式

- 导入 `.docx`
- 导入 `.txt`
- 粘贴或手动输入文本

### 4.2 导入后的处理原则

- 清除原文中的字体、字号、加粗、段落样式、缩进等格式
- 不保留复杂排版
- 不保留图片、表格、页眉页脚、批注、脚注
- 保留纯文字内容和基础段落分隔

### 4.3 编辑能力

编辑窗口需要支持以下基础能力：

- 文本输入
- 文本删除
- 粘贴文本
- 回车分段
- 选中后修改文字
- 撤销/重做
- 全文滚动查看

V1 不支持：

- 富文本局部样式编辑
- 图片插入
- 表格编辑
- 批注、修订、多人协作

### 4.4 排版设置项

用户可设置以下参数：

- 页边距：上、下、左、右，单位为毫米
- 段落行距：固定值，单位为磅
- 文档标题：字体、字号
- 一级标题：字体、字号
- 二级标题：字体、字号
- 正文：字体、字号

V1 内置一套默认模板参数：

- 文档标题：方正小标宋，二号
- 一级标题：黑体，小二
- 二级标题：楷体 GB2312，小二
- 正文：仿宋 GB2312，小二
- 段落行距固定值：30 磅
- 页边距：上 36mm、下 36mm、左 27mm、右 27mm

建议在 V1 中预留但可选实现：

- 是否加粗
- 段前/段后间距
- 首行缩进
- 对齐方式

### 4.5 字体选择策略

- 默认字体下拉框只展示常用字体
- 提供“读取系统已安装字体”的选项
- 若用户选择的字体在当前系统不存在，需给出提示并允许回退到默认字体

### 4.6 输出

V1 仅支持导出：

- `.docx`

V1 不支持：

- PDF 导出
- 一键打印

## 5. 标题识别方案

### 5.1 V1 方案

采用“规则自动识别，后续支持手动修正”的方案。

### 5.2 默认识别规则

- 第一段固定默认识别为文档标题
- 以 `一、二、三……` 开头的段落识别为一级标题
- 以 `（一）（二）（三）……` 开头的段落识别为二级标题
- 其他段落识别为正文

### 5.3 后续增强方向

- 支持用户在界面中将段落类型手动改为 `标题 / 一级标题 / 二级标题 / 正文`
- 支持识别 `1. 2. 3.` 类型标题
- 支持识别 `（1）（2）（3）` 类型标题

## 6. 参数与单位约定

### 6.1 单位定义

- 页边距：毫米（mm）
- 字号：号
- 行距固定值：磅（pt）

### 6.2 内部换算建议

界面展示使用用户熟悉的单位，内部处理时统一换算为 Word 所需参数：

- 毫米转换为 `python-docx` 所需长度单位
- “号”转换为磅值

### 6.3 字号映射表建议

建议内置常见字号映射表，例如：

- 初号 = 42pt
- 小初 = 36pt
- 一号 = 26pt
- 小一 = 24pt
- 二号 = 22pt
- 小二 = 18pt
- 三号 = 16pt
- 小三 = 15pt
- 四号 = 14pt
- 小四 = 12pt
- 五号 = 10.5pt
- 小五 = 9pt

注：最终映射需在实现阶段统一固化，避免前后端含义不一致。

## 7. 界面设计

### 7.1 主界面布局

建议采用左右分栏布局。

左侧区域：

- 文件导入按钮
- 当前文件名显示
- 可视化文本编辑窗口

右侧区域：

- 页边距设置区
- 行距设置区
- 字体字号设置区
- 标题识别结果区
- 导出按钮
- 保存路径选择

### 7.2 交互流程

1. 用户导入文件或粘贴文字
2. 系统提取纯文本并显示到编辑窗口
3. 系统自动识别段落层级
4. 用户修改正文内容和排版参数
5. 用户点击生成文档
6. 系统导出 `.docx`

### 7.3 V1 推荐控件

- 导入按钮
- 多行文本编辑器
- 数值输入框
- 下拉框
- 复选框
- 生成按钮
- 另存为按钮
- 状态提示栏

## 8. 技术方案

### 8.1 技术栈建议

建议采用 Python 技术栈实现 Windows 桌面软件：

- GUI：`PySide6`
- 文档处理：`python-docx`
- 文本规则识别：Python + 正则
- 打包：`PyInstaller`

### 8.2 选型理由

选择 `PySide6` 的原因：

- 适合 Windows 桌面软件
- 原生桌面交互体验较好
- 与 Python 文档处理逻辑集成简单
- 后续打包发布较方便

选择 `python-docx` 的原因：

- 支持创建和导出 `.docx`
- 支持设置页边距
- 支持设置段落与字体样式
- 适合“重新生成文档”的工作流

## 9. 系统模块设计

### 9.1 文件导入模块

职责：

- 选择文件
- 判断文件类型
- 读取 `.txt`
- 读取 `.docx`
- 提取纯文本及段落

输入：

- 文件路径

输出：

- 原始段落列表

### 9.2 文本编辑模块

职责：

- 展示导入内容
- 提供基础编辑能力
- 输出当前编辑后的全文内容

输入：

- 原始段落列表或全文文本

输出：

- 用户编辑后的段落内容

### 9.3 标题识别模块

职责：

- 按规则识别段落类型
- 输出结构化段落数据

输入：

- 段落列表

输出示例：

```json
[
  { "text": "关于XXX工作的通知", "type": "title" },
  { "text": "一、总体要求", "type": "h1" },
  { "text": "现将有关事项通知如下。", "type": "body" },
  { "text": "（一）提高认识", "type": "h2" }
]
```

### 9.4 排版配置模块

职责：

- 管理页边距、行距、字体字号参数
- 做参数校验
- 将“号”等界面值转换为内部值

### 9.5 文档生成模块

职责：

- 创建新文档
- 设置页面边距
- 按段落类型生成内容
- 应用标题/正文样式
- 导出 `.docx`

### 9.6 预览与修正模块

V1 可简化为：

- 显示识别结果列表
- 暂不支持手动修改段落类型

V1.5 或 V2 增强为：

- 用户可直接调整某段的类型

## 10. 数据结构设计

### 10.1 段落结构

```json
[
  {
    "id": "p1",
    "text": "关于XXX工作的通知",
    "type": "title"
  },
  {
    "id": "p2",
    "text": "一、总体要求",
    "type": "h1"
  }
]
```

### 10.2 排版配置结构

```json
{
  "page_margin_mm": {
    "top": 36,
    "bottom": 36,
    "left": 27,
    "right": 27
  },
  "line_spacing_pt": 30,
  "styles": {
    "title": {
      "font_family": "方正小标宋",
      "font_size_hao": "二号"
    },
    "h1": {
      "font_family": "黑体",
      "font_size_hao": "小二"
    },
    "h2": {
      "font_family": "楷体_GB2312",
      "font_size_hao": "小二"
    },
    "body": {
      "font_family": "仿宋_GB2312",
      "font_size_hao": "小二"
    }
  }
}
```

## 11. 非功能需求

### 11.1 可用性

- 界面简洁，普通办公用户可快速上手
- 导入、编辑、导出流程清晰
- 配置项命名尽量贴近办公软件习惯

### 11.2 性能

- 对普通篇幅文档应在可接受时间内完成导入和导出
- 首版可先以中小文档为主，不追求超大文档优化

### 11.3 兼容性

- 运行平台：Windows
- 输入文件：`.docx`、`.txt`
- 输出文件：`.docx`
- 交付方式：免安装绿色版 `.exe` 文件夹

### 11.4 稳定性

- 文件读取失败时需提示明确错误
- 字体不存在时应提示并允许回退
- 导出失败时需提示原因

## 12. 首版边界说明

以下内容明确不纳入 V1：

- PDF 导出
- 保留原 Word 的复杂样式
- 表格、图片、页眉页脚保留
- 批量处理多个文件
- 多模板管理
- 段落类型手动修正
- 高级富文本编辑能力

## 13. 风险与难点

### 13.1 标题识别准确率

单纯依赖规则无法覆盖全部文档写法，因此后续必须预留手动修正能力。

### 13.2 字体依赖

目标字体依赖 Windows 已安装字体。V1 默认只展示常用字体，同时提供“读取系统已安装字体”的选项。若系统不存在指定字体，需提供提示或回退策略。

### 13.3 文档导入预期

用户可能误以为导入后会保留原格式，因此界面和说明文案必须明确“导入后将清除原样式并重新排版”。

### 13.4 编辑器能力边界

V1 编辑窗口主要承担“修改文字内容”的职责，而不是完整 Word 富文本编辑器。

## 14. 开发里程碑

### 里程碑 1：项目骨架

- 初始化桌面项目结构
- 搭建主窗口
- 预留左右布局

### 里程碑 2：导入与编辑

- 支持导入 `.txt`
- 支持导入 `.docx`
- 提取纯文本
- 接入编辑窗口

### 里程碑 3：识别与配置

- 实现标题识别规则
- 完成页边距、行距、字体字号配置面板

### 里程碑 4：导出

- 按配置生成 `.docx`
- 完成保存流程

### 里程碑 5：打磨

- 异常提示
- 参数校验
- 字体回退策略
- 基本可用性优化

## 15. 建议的项目目录

```text
文档排版/
  app/
    main.py
    ui/
    services/
    models/
    utils/
  tests/
  requirements.txt
  build_portable.ps1
  开发文档.md
  界面原型说明.md
  便携版说明.md
```

## 16. 下一步实施建议

建议按以下顺序开发：

1. 先搭建桌面界面骨架和主窗口布局
2. 完成 `.txt` / `.docx` 导入与纯文本提取
3. 接入编辑窗口
4. 完成标题识别模块
5. 完成排版配置面板
6. 完成 `.docx` 导出
7. 最后补异常处理与打包

## 17. 待确认项

以下内容建议在编码前进一步确认：

- 编辑窗口是否需要显示段落序号或识别类型
