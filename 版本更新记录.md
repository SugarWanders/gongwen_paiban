# 公文排版助手版本更新记录

| 项目 | 内容 |
| --- | --- |
| 文档编号 | GWP-REL-001 |
| 文档版本 | V1.1 |
| 更新日期 | 2026-04-20 |
| 适用范围 | 产品版本变化、发布说明、历史记录追踪 |

## V1.1

发布时间：2026-04-20

### 1. 版本标识与发布文件

- 软件版本号由 `V1.0` 更新为 `V1.1`
- 发布目录更新为：`dist/公文排版助手V1_1`
- 主程序更新为：`dist/公文排版助手V1_1/公文排版助手V1_1.exe`
- 为解决 Windows 对旧 exe 文件名图标缓存的问题，正式改用 `公文排版助手V1_1.exe`

### 2. 界面与视觉调整

- 左右主卡片的背景、边框、圆角样式统一
- 右侧参数区补齐与左侧相呼应的主卡片结构
- 右侧绿色卡片的上下间距统一并多次微调
- 黑色 `生成 .docx` 按钮位置调整到导出卡片下方
- 右侧按钮样式调整为与左侧顶部按钮一致
- 右侧参数区整体宽度、边距与留白持续收紧，提升对齐度
- 多处说明文字、单位文字、标签文字进行了像素级对齐调整
- 右下角联系方式位置多次微调
- 左下角状态文字与右下角联系方式在状态栏内重新对齐

### 3. 控件与交互修复

- 页边距与行距数值框改为自定义对齐控件
- 解决数值框中竖线不对齐、上下箭头不对齐、箭头样式不统一的问题
- 字体显示框与字号显示框宽度重新调整
- 字体名称与字号名称在显示框中改为居中显示
- `毫米`、`pt` 与数值框之间的距离按统一网格规则调整
- `字体`、`字号` 与对应显示框之间的距离统一

### 4. 状态栏与工作状态恢复

- 恢复左下角工作状态显示功能，不再只显示固定的“已就绪”
- 重新接回以下状态更新：
  - 导入文件
  - 清空内容
  - 恢复默认参数
  - 设为默认参数
  - 加载本地字体
  - 删除字体
  - 更新默认保存路径
  - 导出成功
- 修复依赖 `QStatusBar.showMessage()` 时中文状态显示异常的问题

### 5. 字体管理功能修复

- 修复 `删除字体` 按键无法打开的问题
- 修复字体选择弹窗参数顺序错误导致的功能异常
- `加载本地字体` 弹窗提示文案调整为：`请选择需要加载的字体，可多选。`
- `删除字体` 弹窗提示文案调整为：`请选择需要删除的字体，可多选。`
- 删除旧版弹窗中冗余、重复或不清晰的提示内容

### 6. 导出与路径逻辑调整

- `恢复默认参数` 时会同步：
  - 恢复默认模板
  - 清空保存路径输入框
  - 删除默认保存路径配置
- 修复并保留导出文档区域的路径输入与选择逻辑

### 7. 图标更新

- 多次更新根目录图标资源：
  - `icon.png`
  - `icon.ico`
- 重新生成多尺寸 `icon.ico`
- 将新图标同步写入：
  - 根目录图标文件
  - 打包后的 `_internal` 图标资源
  - exe 文件本身图标资源
- 通过更换程序文件名为 `公文排版助手V1_1.exe` 解决旧图标缓存问题

### 8. 文档整理

- 删除了旧版、乱码或与当前版本不一致的说明资料
- 重新补齐并统一了当前产品文档：
  - `README.md`
  - `开发文档.md`
  - `用户使用说明.md`
  - `界面设计规范.md`
  - `版本更新记录.md`

### 9. 版本说明

V1.1 是在 V1.0 基础上的一次界面精修与可用性修复版本，重点解决了以下问题：

- 图标更新不生效
- 界面多处对齐不一致
- 状态栏工作状态丢失
- 字体管理功能异常
- 旧文档内容与当前软件不一致

---

## V1.0

### 1. 首个可用版本

- 实现文本导入、粘贴与编辑
- 实现标题自动识别
- 实现公文参数设置
- 实现字体与字号设置
- 实现导出 `.docx`
- 实现默认模板保存与恢复
- 实现默认保存路径逻辑
- 实现本地字体加载与软件字体删除
- 完成 Windows 便携版打包

### 2. 默认参数

- 上边距：36 mm
- 下边距：36 mm
- 左边距：27 mm
- 右边距：27 mm
- 固定行距：30 pt
- 文档标题：方正小标宋简体，二号
- 一级标题：黑体，小二
- 二级标题：楷体_GB2312，小二
- 正文：仿宋_GB2312，小二

---

后续新版本建议按以下格式追加：

```md
## V1.x

发布时间：YYYY-MM-DD

### 1. 版本标识与发布文件
### 2. 界面与视觉调整
### 3. 功能修复
### 4. 文档与发布说明
```
