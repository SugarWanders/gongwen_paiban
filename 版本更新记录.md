# 版本更新记录

| 项目 | 内容 |
| --- | --- |
| 文档编号 | GWP-REL-001 |
| 文档版本 | V1.3.1 |
| 更新日期 | 2026-05-27 |
| 适用范围 | 公文排版助手版本变更说明 |

## V1.3.1

发布日期：2026-05-26

覆盖更新时间：2026-05-27

### 主要更新

- 版本号更新为 V1.3.1，打包目录和主程序名称统一为 `公文排版助手V1_3_1`。
- 新增“重点字段”功能，支持默认字段、用户新增字段、删除字段和持久化保存。
- 默认重点字段为“分析认为”“一是”“二是”“三是”“四是”“五是”。
- 重点字段支持独立设置字体、字号和加粗状态。
- 默认重点字段格式为 `仿宋`、`小二`、加粗。
- 导出 `.docx` 时匹配重点字段并应用专用格式。
- 输出正文段落改为两端对齐。
- 输出文档标题后自动增加一个空白段落，空段落无文字，格式沿用正文。
- 修正中英文标点自动转换规则，支持中文语境和英文语境自动适配。
- 默认字体调整为：
  - 文档标题：黑体，二号
  - 一级标题：黑体，小二
  - 二级标题：黑体，小二
  - 正文：仿宋，小二
- 字体由用户电脑本地字体提供，导出的 `.docx` 写入真实字体名称。
- 加载本地字体列表直接读取 Windows 本地字体。
- 删除字体列表仅隐藏软件下拉菜单中的字体选项。
- 界面中字体名称直接显示为真实字体名，不显示额外标记。
- 2026-05-27 覆盖修订：取消历史字体别名替换规则，`方正小标宋简体`、`方正小标宋_GBK`、`楷体_GB2312`、`仿宋_GB2312` 不再被自动替换为 `黑体` 或 `仿宋`。
- 2026-05-27 覆盖修订：加载本地字体、字体下拉框和 `.docx` 导出均保留用户电脑中的真实字体名。
- 加载字体、删除字体和重点字段弹窗风格统一。
- 用户修改排版参数后自动保存，下次启动继续使用上次设置。
- 构建脚本输出目录改为 `dist`，并清理旧版本目录和运行配置目录。

### 交付形式

- 本地输出目录：`dist/公文排版助手V1_3_1`
- 主程序：`公文排版助手V1_3_1.exe`
- 推荐 Release 附件：`公文排版助手V1_3_1-windows-portable.zip`

## V1.3

发布日期：2026-05-12

### 主要更新

- 主界面升级为更舒展的 `1030 x 613` 布局。
- 字体、按钮、输入框、下拉框、页边距控件和区域间距重新校准。
- 生成按钮改为深灰绿色 `#5F7450`。
- 修复外接显示器、复制投影和不同 DPI 缩放下的文字定位问题。
- 修复“正文”标题和底部联系方式在特殊显示模式下跑偏的问题。
- 默认字体调整为文档标题、二级标题、正文使用仿宋，一级标题使用黑体。
- 固定行距单位显示为“磅”。
- GitHub Actions 支持自动编译 Windows 便携版并发布 Release。

## V1.2.1

发布日期：2026-05-04

### 主要更新

- 导入文档后，默认导出路径自动设置为导入文件所在文件夹。
- 默认字体调整为黑体和仿宋组合。
- README 和 Release 说明同步更新到 V1.2.1。
- 修复发布说明与仓库分支历史不一致的问题。

## V1.2

发布日期：2026-04-29

### 主要更新

- 界面进一步压缩为紧凑布局。
- 页面设置、段落设置和字体字号区域重新排版。
- 导出文档路径和生成按钮位置调整。
- 默认导出文件名采用“原文件标题 + _排版结果”的规则。
- 导出成功后提供“打开文档”和“在文件夹中显示”操作。
- 合并新的 `dist` 目录，清理无用构建文件。

## V1.1

发布日期：2026-04-20

### 主要更新

- 软件名称、版本号和输出目录从 V1.0 更新为 V1.1。
- 优化 Windows 便携版打包结构。
- 改进应用图标、窗口标题和基础界面样式。
- 修复部分导出文档格式问题。

## V1.0

发布日期：2026-04-17

### 首版功能

- 支持导入 `.docx` 和 `.txt`。
- 支持在编辑区直接粘贴或输入文字。
- 支持清除导入文档原格式。
- 支持自动识别文档标题、一级标题、二级标题和正文。
- 支持设置页边距、固定行距、字体和字号。
- 支持导出 `.docx` 文档。
- 支持 Windows 便携版运行。
