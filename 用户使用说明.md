# 公文排版助手 V1.1 用户使用说明

| 项目 | 内容 |
| --- | --- |
| 文档编号 | GWP-USER-001 |
| 文档版本 | V1.1 |
| 更新日期 | 2026-04-20 |
| 适用范围 | 最终用户使用、基础操作、常见问题 |

## 1. 软件简介

公文排版助手 V1.1 是一款 Windows 桌面便携软件，用于将导入或粘贴的文本按公文格式重新排版，并导出为 `.docx` 文档。

软件无需安装，直接打开 `公文排版助手V1_1.exe` 即可使用。

## 2. 启动方式

程序路径：

```text
dist/公文排版助手V1_1/公文排版助手V1_1.exe
```

双击后即可启动软件。

## 3. 主界面说明

### 3.1 左侧区域

- `导入文件`
  - 导入 `.docx` 或 `.txt`
- `清空内容`
  - 清空当前编辑内容，并恢复默认说明文字
- 编辑区
  - 可直接粘贴或输入文本
  - 导入内容也会显示在此区域
- 编辑区底部统计信息
  - 段落数
  - 字数
  - 识别状态

### 3.2 右侧区域

右侧区域用于设置排版参数与导出文档，包括以下功能：

- 恢复默认参数
- 当前参数设为默认值
- 加载本地字体
- 删除字体
- 页面设置
- 段落设置
- 文档标题
- 一级标题
- 二级标题
- 正文
- 导出文档
- 生成 `.docx`

### 3.3 底部状态栏

- 左下角：显示软件当前工作状态
- 右下角：显示联系方式

## 4. 基本操作流程

### 4.1 导入文档

1. 点击 `导入文件`
2. 选择 `.docx` 或 `.txt`
3. 软件会自动清除原有格式，仅保留文字和段落结构
4. 内容会显示在左侧编辑区

### 4.2 直接粘贴文本

1. 将光标置于左侧编辑区
2. 直接粘贴或输入文本
3. 软件会按规则自动识别标题层级

### 4.3 设置排版参数

根据需要调整以下参数：

- 页边距
- 固定行距
- 文档标题字体与字号
- 一级标题字体与字号
- 二级标题字体与字号
- 正文字体与字号

### 4.4 选择保存位置

1. 点击 `选择保存位置`
2. 选择一个文件夹
3. 软件会询问是否将该文件夹设为默认保存路径

### 4.5 导出文档

1. 确认左侧已有正式内容
2. 确认右侧参数设置完成
3. 点击 `生成 .docx`
4. 导出成功后，可选择：
   - 打开文档
   - 在文件夹中显示

## 5. 自动识别规则

软件当前使用以下识别规则：

- 第一段识别为文档标题
- 以“一、二、三”开头识别为一级标题
- 以“（一）（二）（三）”开头识别为二级标题
- 其他段落识别为正文

## 6. 默认参数

当前默认值如下：

- 上边距：36 mm
- 下边距：36 mm
- 左边距：27 mm
- 右边距：27 mm
- 固定行距：30 pt
- 文档标题：方正小标宋简体，二号
- 一级标题：黑体，小二
- 二级标题：楷体_GB2312，小二
- 正文：仿宋_GB2312，小二

## 7. 字体功能说明

### 7.1 加载本地字体

点击 `加载本地字体` 后，会打开字体选择窗口。

提示文字为：

`请选择需要加载的字体，可多选。`

功能说明：

- 字体来源于 `C:\Windows\Fonts`
- 支持多选
- 加载后会加入软件字体下拉菜单

### 7.2 删除字体

点击 `删除字体` 后，会打开删除窗口。

提示文字为：

`请选择需要删除的字体，可多选。`

功能说明：

- 支持多选
- 删除的是软件中的字体选项，不会删除系统字体文件

## 8. 默认参数与路径说明

### 8.1 恢复默认参数

点击后会执行以下操作：

- 恢复默认模板参数
- 清空保存路径输入框
- 清除默认保存路径配置

### 8.2 当前参数设为默认值

点击后会将当前参数保存下来，下次打开软件会自动加载。

## 9. 导出结果说明

导出文件名规则如下：

```text
原文标题_排版结果.docx
```

如目标文件夹中已存在同名文件，软件会自动追加数字，例如：

```text
原文标题_排版结果1.docx
原文标题_排版结果2.docx
```

## 10. 状态栏说明

左下角状态栏会根据当前操作动态变化，例如：

- 公文排版助手已就绪。
- 已导入文件：xxx.docx
- 编辑内容已恢复为默认说明。
- 已恢复默认参数，并清空保存路径。
- 默认参数已保存：...
- 已加载 3 个本地字体。
- 已删除 2 个字体选项。
- 导出成功：...

## 11. 常见问题

### 11.1 为什么不能导出？

常见原因包括：

- 编辑区没有正式内容
- 保存路径未选择
- 缺少导出依赖

### 11.2 删除字体会不会影响电脑？

不会。  
删除的是软件下拉列表中的字体选项，不会删除 Windows 系统里的字体文件。

### 11.3 为什么导入后原格式没有保留？

这是软件的正常设计。  
软件会清除原文样式，只保留文字和段落结构，再按当前公文参数重新排版。

### 11.4 为什么导出的文件名会自动变化？

如果目标文件夹中已经存在同名文件，软件会自动追加数字，以避免覆盖旧文件。

## 12. 联系方式

如有问题或建议，请联系：

`sugarwanders@163.com`
