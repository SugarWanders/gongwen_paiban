# 用户使用说明

| 项目 | 内容 |
| --- | --- |
| 文档编号 | GWP-USER-001 |
| 文档版本 | V1.3.1 |
| 更新日期 | 2026-05-26 |
| 适用范围 | 公文排版助手普通用户操作说明 |

## 软件用途

公文排版助手用于把已有文字内容重新整理为统一格式，并导出为 `.docx` 文档。用户可以导入 `.docx`、`.txt`，也可以直接在编辑区粘贴文字。

## 下载与运行

1. 获取 `公文排版助手V1_3_1-windows-portable.zip`。
2. 解压压缩包。
3. 双击 `公文排版助手V1_3_1.exe`。

该版本是便携版，不需要安装 Python 或其他开发依赖。

## 主界面说明

- 左侧编辑区：导入、粘贴、查看和修改文字。
- 左上角按钮：导入文件、清空内容。
- 左上角右侧按钮：重点字段。
- 右侧参数区：设置页面、段落、字体、字号和导出路径。
- 底部状态区：显示段落数、字数、识别状态和提示信息。

## 基本使用流程

### 1. 输入文字

可以选择以下任一方式：

- 点击“导入文件”，选择 `.docx` 或 `.txt` 文件。
- 直接在左侧编辑区粘贴或输入文字。

导入文档后，软件会自动清除原格式，仅保留文字和段落结构。

### 2. 检查和编辑内容

导入或粘贴后，可以在左侧编辑区直接修改文字。软件支持普通文本编辑、删除、粘贴和回车分段。

### 3. 设置排版参数

右侧可以设置：

- 上、下、左、右页边距，单位为“毫米”。
- 固定行距，单位为“磅”。
- 文档标题字体和字号。
- 一级标题字体和字号。
- 二级标题字体和字号。
- 正文字体和字号。

默认参数：

- 页边距：上 36、下 36、左 27、右 27 毫米。
- 固定行距：30 磅。
- 文档标题：方正小标宋简体，二号。
- 一级标题：黑体，小二。
- 二级标题：楷体_GB2312，小二。
- 正文：仿宋_GB2312，小二。

### 4. 设置重点字段

点击右上角“重点字段”按钮，可以设置需要在正文中重点强调的文字。

默认重点字段包括：

- 分析认为
- 一是
- 二是
- 三是
- 四是
- 五是

重点字段弹窗支持：

- 增加重点字段。
- 删除已有重点字段。
- 设置重点字段专用字体。
- 设置重点字段专用字号。
- 勾选或取消加粗。
- 保存后下次打开软件继续使用上次设置。

默认重点字段格式为 `仿宋_GB2312`、`小二`、加粗。

### 5. 保存默认参数

设置好参数后，可以点击“当前参数设为默认值”。下次打开软件时，会优先使用保存后的参数。

如果需要恢复软件内置参数，点击“恢复默认参数”。恢复默认参数也会回到 V1.3.1 内置字体组合。

### 6. 加载或删除字体

软件已经随包内置以下字体：

- 方正小标宋简体
- 黑体
- 楷体_GB2312
- 仿宋_GB2312

这些字体会在软件启动时自动注册，界面下拉框中直接显示字体名。

点击“加载本地字体”后，软件会读取电脑中的字体列表。软件自带字体不会出现在该面板中，避免重复加载。

点击“删除字体”可以从软件字体下拉菜单中隐藏不需要显示的字体名称。删除只是隐藏软件中的字体选项，不会删除电脑或软件包里的字体文件。

### 7. 选择保存位置

点击“选择保存位置”，选择导出文件夹。

第一次选择保存位置后，软件会询问是否设为默认保存路径：

- 点击“是”：下次打开软件会自动显示该文件夹。
- 点击“否”：只用于本次导出。

默认保存路径只保存文件夹，不保存具体文件名。

### 8. 导出文档

点击“生成 .docx”后，软件会导出排版后的文档。

导出文件名规则：

```text
原文件标题_排版结果.docx
```

如果文件名重复，会自动追加数字后缀。

导出时会自动处理：

- 文档标题居中。
- 标题后空一行。
- 一级标题、二级标题按规则设置缩进和字体。
- 正文两端对齐。
- 重点字段应用专用格式。
- 中文语境使用中文标点，英文语境使用英文标点。

导出成功后，可以选择：

- 打开文档。
- 在文件夹中显示。
- 直接关闭弹窗。

## 自动识别规则

软件按以下规则识别段落：

- 第一段识别为文档标题。
- 以“一、二、三”等开头的段落识别为一级标题。
- 以“（一）（二）（三）”等开头的段落识别为二级标题。
- 其他段落识别为正文。

## 常见问题

### 文档里的字体没有正确显示怎么办？

V1.3.1 已经随软件打包常用公文字体。若仍有其他自定义字体显示异常，请确认电脑中安装了对应字体，或点击“加载本地字体”把本机字体加入下拉菜单。

### 删除字体会删掉字体文件吗？

不会。删除字体只会把字体名称从软件下拉菜单中隐藏，不会删除系统字体，也不会删除软件自带字体文件。

### 为什么导出的文档和原文档格式不同？

这是正常行为。软件会清除导入文档原格式，然后按当前参数重新排版。

### 为什么英文名称后面的句号会变成中文句号？

如果英文名称位于中文句子中，句末标点会按中文语境处理，例如 `“Codepilot”。`。如果整句为英文，则使用英文标点。

### 普通用户需要安装依赖吗？

不需要。Windows 便携版解压后直接双击 exe 即可运行。
