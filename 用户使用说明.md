# 用户使用说明

| 项目 | 内容 |
| --- | --- |
| 文档编号 | GWP-USER-001 |
| 文档版本 | V1.3 |
| 更新日期 | 2026-05-13 |
| 适用范围 | 公文排版助手普通用户操作说明 |

## 软件用途

公文排版助手用于把已有文字内容重新整理为统一格式，并导出为 `.docx` 文档。用户可以导入 `.docx`、`.txt`，也可以直接在编辑区粘贴文字。

## 下载与运行

1. 打开 GitHub Release 页面。
2. 下载 `公文排版助手V1_3-windows-portable.zip`。
3. 解压压缩包。
4. 双击 `公文排版助手V1_3.exe`。

该版本是便携版，不需要安装 Python 或其他依赖。

## 主界面说明

主界面分为三部分：

- 左侧编辑区：导入、粘贴、查看和修改文字。
- 右侧参数区：设置页面、段落、字体、字号和导出路径。
- 底部状态区：显示当前状态和联系邮箱。

## 基本使用流程

### 1. 输入文字

可以选择以下任一方式：

- 点击“导入文档”，选择 `.docx` 或 `.txt` 文件。
- 直接在左侧编辑区粘贴或输入文字。

导入文档后，软件会自动清除原格式，仅保留文字和段落结构。

### 2. 检查和编辑内容

导入或粘贴后，可以在左侧编辑区直接修改文字。支持普通文本编辑、删除、粘贴和回车分段。

### 3. 设置排版参数

右侧可以设置：

- 上、下、左、右页边距，单位为“毫米”
- 固定行距，单位为“磅”
- 文档标题字体和字号
- 一级标题字体和字号
- 二级标题字体和字号
- 正文字体和字号

默认参数：

- 页边距：上 36、下 36、左 27、右 27 毫米
- 固定行距：30 磅
- 文档标题：仿宋，二号
- 一级标题：黑体，小二
- 二级标题：仿宋，小二
- 正文：仿宋，小二

### 4. 保存默认参数

设置好参数后，可以点击“当前参数设为默认值”。下次打开软件时，会优先使用保存后的参数。

如果需要恢复软件内置参数，点击“恢复默认参数”。

### 5. 加载或删除字体

点击“加载本地字体”后，软件会读取电脑中的字体列表。中文名称字体会排在前面，英文名称字体会排在后面。

选择需要的字体后，它们会加入字体下拉菜单。

点击“删除字体”可以从软件字体选项中移除不需要显示的字体名称。

### 6. 选择保存位置

点击“选择保存位置”，选择导出文件夹。

第一次选择保存位置后，软件会询问是否设为默认保存路径：

- 点“是”：下次打开软件会自动显示该文件夹。
- 点“否”：只用于本次导出。

默认保存路径只保存文件夹，不保存具体文件名。

### 7. 导出文档

点击“生成 .docx”后，软件会导出排版后的文档。

导出文件名规则：

```text
原文件标题_排版结果.docx
```

如果文件名重复，会自动追加数字后缀。

导出成功后，可以选择：

- 打开文档
- 在文件夹中显示
- 直接关闭弹窗

## 自动识别规则

软件按以下规则识别段落：

- 第一段识别为文档标题。
- 以“一、二、三”等开头的段落识别为一级标题。
- 以“（一）（二）（三）”等开头的段落识别为二级标题。
- 其他段落识别为正文。

## 常见问题

### 文档里的字体没有正确显示怎么办？

请确认电脑中安装了对应字体。如果缺少字体，可以安装字体后重新打开软件，或点击“加载本地字体”把本机字体加入下拉菜单。

### 为什么导出的文档和原文档格式不同？

这是正常行为。软件会清除导入文档原格式，然后按当前参数重新排版。

### 为什么看不到新图标？

Windows 资源管理器可能缓存旧图标。可以尝试关闭旧程序、重新解压新版，或把程序放到新的文件夹后再打开。

### 普通用户需要安装依赖吗？

不需要。Release 附件是 Windows 便携版，解压后直接双击 exe 即可运行。
