# 文档重排版工具界面原型说明

## 1. 原型目标

本说明用于明确 V1 桌面界面的结构、交互逻辑和各区域职责，作为后续使用 `PySide6` 搭建界面的直接依据。

V1 的界面目标是：

- 让用户可以快速导入或粘贴内容
- 让用户可以在一个清晰的编辑窗口内修改文字
- 让用户可以在同一窗口完成排版参数设置
- 让用户以最少步骤生成 `.docx`

软件发布目标为免安装绿色版，用户双击程序即可打开主窗口。

## 2. 主窗口结构

主窗口采用左右分栏布局，建议默认宽屏显示。

### 2.1 左侧内容区

左侧占主界面约 60% 到 70% 宽度，承担“内容输入和编辑”的核心职责。

包含以下区域：

- 顶部工具条
- 文件信息条
- 可视化文本编辑区
- 底部状态栏

### 2.2 右侧配置区

右侧占主界面约 30% 到 40% 宽度，承担“排版参数设置和导出操作”的职责。

包含以下区域：

- 默认模板卡片
- 页面设置卡片
- 段落设置卡片
- 样式设置卡片
- 字体扩展选项
- 标题识别说明区
- 导出操作区

## 3. 左侧内容区原型

### 3.1 顶部工具条

建议从左到右放置：

- `导入文件` 按钮
- `粘贴文本` 按钮
- `清空内容` 按钮
- `恢复默认模板` 按钮

交互说明：

- 点击 `导入文件` 后弹出文件选择框，仅允许选择 `.docx` 和 `.txt`
- 点击 `粘贴文本` 时，焦点进入编辑区，便于直接粘贴
- 点击 `清空内容` 前需二次确认
- 点击 `恢复默认模板` 时，仅恢复右侧配置，不清空左侧文本

### 3.2 文件信息条

建议显示：

- 当前文件名
- 文件来源状态
- 导入提示语

示例：

- 当前文件：`通知草稿.docx`
- 状态：已导入，原格式已清除

交互说明：

- 如果内容来自文本框手工输入，则显示“未导入文件”
- 每次导入成功后更新该区域

### 3.3 可视化文本编辑区

这是 V1 的主编辑区域，建议使用支持多段文本编辑的纯文本编辑器控件。

设计要求：

- 占据左侧大部分空间
- 支持滚动浏览
- 支持多段文本编辑
- 支持撤销/重做
- 支持复制、粘贴、剪切、全选
- 支持显示插入光标和文本选区

V1 中的“可视化”定义为：

- 用户能直接看到完整文本内容
- 用户能像普通编辑器一样进行基础编辑
- 不要求做富文本局部样式预览

内容展示规则：

- 导入 `.docx` 后，去除原样式，只按段落换行展示文本
- 导入 `.txt` 后，尽量保留换行结构
- 手动输入时实时更新内容

### 3.4 底部状态栏

建议显示：

- 总段落数
- 总字数
- 当前识别状态
- 最近一次操作结果

示例：

- 段落数：18
- 字数：3246
- 识别状态：已自动识别
- 提示：导出成功

## 4. 右侧配置区原型

### 4.1 默认模板卡片

位置在右侧最上方，用于展示当前默认模板。

建议显示：

- 模板名称：`内置公文模板`
- 文档标题：方正小标宋，二号
- 一级标题：黑体，小二
- 二级标题：楷体 GB2312，小二
- 正文：仿宋 GB2312，小二
- 行距：30 磅
- 页边距：上 36，下 36，左 27，右 27 mm

建议提供：

- `恢复默认模板` 按钮

### 4.2 页面设置卡片

包含四个输入项：

- 上边距
- 下边距
- 左边距
- 右边距

控件建议：

- 数值输入框
- 单位标签固定显示 `mm`

交互规则：

- 只允许输入正数
- 支持上下微调按钮
- 失焦后自动校验

### 4.3 段落设置卡片

V1 先保留一个核心参数：

- 固定行距

控件建议：

- 数值输入框
- 单位标签固定显示 `pt`

默认值：

- `30`

### 4.4 样式设置卡片

建议按四组分别显示：

- 文档标题
- 一级标题
- 二级标题
- 正文

每组包含：

- 字体下拉框
- 字号下拉框

字号下拉框建议提供常见选项：

- 初号
- 小初
- 一号
- 小一
- 二号
- 小二
- 三号
- 小三
- 四号
- 小四
- 五号
- 小五

默认值：

- 文档标题：方正小标宋，二号
- 一级标题：黑体，小二
- 二级标题：楷体 GB2312，小二
- 正文：仿宋 GB2312，小二

### 4.5 字体扩展选项

该区域用于处理“常用字体”和“系统字体”的切换。

建议提供：

- `仅显示常用字体` 单选项
- `读取系统已安装字体` 单选项或复选项
- `重新加载字体列表` 按钮

常用字体建议首版内置：

- 方正小标宋
- 黑体
- 楷体 GB2312
- 仿宋 GB2312
- 宋体
- 仿宋
- 楷体
- 微软雅黑

交互规则：

- 默认仅显示常用字体
- 勾选或切换后再读取系统字体
- 若系统字体读取失败，应提示并保留常用字体列表

### 4.6 标题识别说明区

V1 暂不做手动段落修正，但建议保留一个只读说明区，告诉用户当前识别逻辑。

建议显示：

- 第一段识别为文档标题
- `一、二、三` 识别为一级标题
- `（一）（二）（三）` 识别为二级标题
- 其他段落识别为正文

建议附加一个状态提示：

- `当前内容将在导出时按上述规则自动套用样式`

### 4.7 导出操作区

这是右侧最下方的核心操作区域。

建议包含：

- 保存路径输入框
- `选择保存位置` 按钮
- `生成 .docx` 主按钮

交互规则：

- 若没有文本内容，生成按钮置灰
- 若保存路径为空，点击生成时先提示选择路径
- 生成成功后提示文件位置

## 5. 推荐交互流程

### 5.1 从文件导入开始

1. 用户点击 `导入文件`
2. 选择 `.docx` 或 `.txt`
3. 系统读取文本并清除原样式
4. 内容显示到左侧编辑区
5. 右侧保持默认模板参数
6. 用户视情况修改文字或参数
7. 点击 `生成 .docx`
8. 系统导出文件并提示成功

### 5.2 从粘贴文本开始

1. 用户直接在编辑区粘贴或输入文字
2. 用户检查右侧模板参数
3. 如有需要调整页边距、行距、字体字号
4. 点击 `生成 .docx`
5. 系统导出文件

## 6. 状态与异常提示设计

### 6.1 正常状态提示

建议使用底部状态栏或轻量提示框展示：

- 导入成功
- 已清除原格式
- 已恢复默认模板
- 导出成功

### 6.2 异常提示

需要明确提示的情况：

- 文件格式不支持
- 文件读取失败
- `.docx` 内容为空
- 保存失败
- 字体不存在
- 参数填写不合法

提示文案应尽量直接，例如：

- `只能导入 .docx 或 .txt 文件`
- `文档读取失败，请确认文件未损坏`
- `当前字体在本机不存在，已回退为默认字体`

## 7. V1 界面边界

V1 界面中明确不做：

- 所见即所得的 Word 样式预览
- 段落拖拽排序
- 段落类型手动切换
- 多标签文档编辑
- 表格和图片编辑区
- 历史版本管理

## 8. PySide6 落地建议

建议控件映射如下：

- 主窗口：`QMainWindow`
- 主容器：`QSplitter` 或左右 `QHBoxLayout`
- 编辑区：`QPlainTextEdit`
- 配置卡片：`QGroupBox`
- 数值输入：`QSpinBox` / `QDoubleSpinBox`
- 字体下拉：`QComboBox`
- 操作按钮：`QPushButton`
- 状态栏：`QStatusBar`
- 文件选择：`QFileDialog`

## 9. 后续增强接口预留

界面设计时建议预留以下扩展空间：

- 在编辑区旁增加“段落类型”列表
- 在右侧增加“手动修正段落类型”区
- 增加“模板另存为”功能
- 增加“导出 PDF”功能

## 10. 下一步建议

完成这份原型说明后，建议直接进入项目骨架搭建，优先实现：

1. 主窗口左右布局
2. 文本编辑区
3. 右侧参数面板
4. 文件导入按钮与保存按钮占位
