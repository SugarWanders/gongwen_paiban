# 界面设计规范

| 项目 | 内容 |
| --- | --- |
| 文档编号 | GWP-UI-001 |
| 文档版本 | V1.3.1 |
| 更新日期 | 2026-05-26 |
| 适用范围 | 公文排版助手界面布局、视觉样式和交互规范 |

## 设计目标

公文排版助手是面向办公排版的 Windows 桌面工具。界面应保持安静、清晰、紧凑和易重复操作。设计重点不是展示型首页，而是打开即可完成导入、编辑、设置参数、设置重点字段和导出文档。

## 整体布局

- 默认窗口尺寸：`1030 x 613`
- 左侧：文本导入、粘贴、编辑区域
- 右侧：参数设置区域
- 底部：状态信息和联系方式
- 右侧参数面板宽度：`390px`
- 主卡片高度：`572px`

界面采用左右双区域布局。左侧保留较大的编辑空间，右侧集中放置页面设置、段落设置、字体字号、导出文档和生成按钮。

## 字体规范

界面字体优先使用系统常见办公字体：

```text
Microsoft YaHei UI
Segoe UI
SimHei
Arial
```

当用户电脑没有首选界面字体时，系统自动使用后续字体作为 fallback。

主要字号：

- 全局基础字号：`10pt`
- 编辑区正文：`11pt`
- 分区标题：`10.5pt`
- 右侧参数控件：`9pt`
- 底部状态文字：`9.5pt`

## 色彩规范

### 背景与卡片

- 页面背景：`#A6B1A3`
- 主卡片背景：`#F4F7F1`
- 主卡片边框：`#CFD8CA`
- 设置卡片背景：`#CFE89A`
- 设置卡片边框：`#B7D97A`

### 主操作按钮

- 默认背景：`#5F7450`
- 悬停背景：`#6C835B`
- 按下背景：`#4E6042`
- 边框颜色：`#4A5D3E`
- 文字颜色：`#FFFFFF`

### 输入控件

- 输入框背景：`#FCFDFB`
- 输入框边框：`#C5D0BF`
- 聚焦边框：`#AFC861`
- 选择高亮：`#D7F95A`

### 弹窗列表

- 列表背景：`#F7FAF4`
- 列表边框：`#D7E5D0`
- 选中背景：`#D7F95A`
- 悬停背景：`#ECF6CC`

## 控件规范

### 按钮

- 普通按钮使用浅色背景、深色文字和圆角边框。
- “生成 .docx”按钮使用主操作绿色。
- 同一区域内按钮高度应保持一致。
- 按钮文字在控件内水平和垂直居中。

### 下拉框

- 字体下拉框宽度保持 V1.3 原始尺寸，不因字体来源而额外加长。
- 字体名称直接显示真实字体名，不显示额外标识。
- 字号下拉框宽度保持统一。
- 下拉箭头统一使用 `app/ui/assets/down-arrow.svg`。
- 鼠标悬停和选中时，选项文字仍保持深色，避免白底白字或浅底浅字。

### 数字选择框

- 页边距和固定行距使用统一的自定义 `AlignedSpinBox`。
- 数字在输入区居中。
- 上下箭头使用同一套 SVG 资源。
- 箭头列和中间分隔线保持垂直对齐。

## 参数区规范

右侧参数区按以下顺序排列：

1. 恢复默认参数、当前参数设为默认值、加载本地字体、删除字体
2. 页面设置
3. 段落设置
4. 文档标题
5. 一级标题
6. 二级标题
7. 正文
8. 导出文档
9. 生成 `.docx` 按钮

页面设置中的单位统一使用“毫米”。段落设置中的固定行距单位统一使用“磅”。

## 编辑区规范

默认说明文字使用浅灰绿色，用户开始编辑后切换为正文深色。默认说明内容应说明：

- 可以直接粘贴或输入文字。
- 导入文档会清除原有格式。
- 自动识别规则。
- 重点字段的默认内容和修改入口。
- 需要确保电脑中有相应字体。

## 重点字段弹窗规范

重点字段弹窗应与其他弹窗风格一致：

- 白色弹窗背景。
- 浅色内容面板。
- 浅绿边框。
- 同一套按钮样式。
- 同一套字体体系。

弹窗内容：

- 顶部标题“重点字段”。
- 右上角“增加重点字段”按钮。
- 默认显示六个重点字段。
- 每个重点字段右侧都有“删除”按钮。
- 字段较多时，弹窗高度可随内容扩展，并通过滚动区域承载列表。
- 专用格式区域包含字体、字号和加粗选项。
- 字体和字号内容在显示框中居中显示。
- 字体下拉框直接显示真实字体名。

## 字体管理规范

界面中的字体直接显示真实字体名，不显示额外说明。

“加载本地字体”弹窗读取 Windows 本地字体列表，供用户加入软件字体下拉菜单。

“删除字体”弹窗可隐藏字体下拉菜单选项。删除行为仅隐藏软件选项，不删除系统字体文件。

## DPI 与多显示器规范

应用启动时应启用 Windows Per-Monitor DPI Awareness，避免在笔记本外接显示器、复制投影、仅第二屏幕和不同缩放比例下出现文字裁切或位置漂移。

涉及中文标签、固定宽度控件和像素级对齐时，应使用 Qt 字体度量进行校验：

- `fontMetrics().horizontalAdvance(text)`
- `fontMetrics().boundingRect(text).width()`

当截图对标要求较细时，应先生成真实界面预览图和测量报告，再修改正式代码。

## 预览与验收

界面修改应优先生成真实应用截图，而不是手工绘制图片。预览文件建议保存到：

```text
ui_measurements/
```

测量报告应包含：

- 目标窗口尺寸。
- 设备像素比。
- 关键控件坐标和尺寸。
- 标签字体宽度校验。
- 是否存在裁切风险。

这些预览和测量文件属于生成物，提交前可删除。
